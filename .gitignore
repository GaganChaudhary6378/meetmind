# Secrets — never push
.env
*.env.local
credentials.json
token.json
*.pem
*.key

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.pytest_cache/
*.egg-info/
.coverage
htmlcov/

# Voice loop model weights — large binaries, fetched via README §7
# (kokoro-v1.0.onnx is ~325MB, over GitHub's 100MB push limit)
/kokoro-v1.0.onnx
/voices-v1.0.bin

# OS / editor junk
.DS_Store
.idea/
.vscode/
.gstack/
.serena/
